const ClaimDisplay = () => {
    const claims = getAllClaims();
    console.log(claims);
    return(
        <table>
            <tbody>
                {claims.map}
            </tbody>
        </table>
    )
};
export default ClaimDisplay;